<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.libsystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"

        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutForm"></activity>
        <activity android:name=".ViewBooks1" />
        <activity android:name=".ViewBooks" />
        <activity android:name=".UserLogin" />
        <activity android:name=".UserProfile" />
        <activity android:name=".UserRegisteration" />
        <activity android:name=".UserInfo" />
        <activity android:name=".BookInfo" />
        <activity android:name=".AdminOperations" />
        <activity android:name=".AdminLogin1" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>